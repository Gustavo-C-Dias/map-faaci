generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Point {
  id        Int     @id @default(autoincrement())
  name      String
  latitude  Float
  longitude Float
}